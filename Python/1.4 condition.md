# ✅ 1) What is a Condition?

A condition helps your program **make decisions**.

Example in real life:

* If it’s raining → take an umbrella
* Else → don’t take it

---

# ✅ 2) `if` Statement (Basic)

### ✅ Syntax

```python
if condition:
    # code runs if condition is True
```

### ✅ Example

```python
age = 20

if age >= 18:
    print("You are allowed to vote ✅")
```

---

# ✅ 3) `if-else`

### ✅ Syntax

```python
if condition:
    # True block
else:
    # False block
```

### ✅ Example

```python
age = 16

if age >= 18:
    print("Adult ✅")
else:
    print("Not adult ❌")
```

---

# ✅ 4) `if-elif-else` (Multiple conditions)

### ✅ Example (Grading System)

```python
marks = 75

if marks >= 80:
    print("A+")
elif marks >= 70:
    print("A")
elif marks >= 60:
    print("A-")
else:
    print("Fail")
```

✅ Python checks **top to bottom**, first match will run.

---

# ✅ 5) Using Conditions with `input()`

```python
age = int(input("Enter your age: "))

if age >= 18:
    print("You can register ✅")
else:
    print("You are too young ❌")
```

---

# ✅ 6) Comparison Operators Used in Conditions ✅

You’ll mostly use these:

```python
==   # equal
!=   # not equal
>    # greater
<    # less
>=   # greater or equal
<=   # less or equal
```

---

# ✅ 7) Logical Operators in Conditions ✅

### ✅ `and` (both must be True)

```python
age = 20
has_nid = True

if age >= 18 and has_nid:
    print("You can create account ✅")
else:
    print("Not allowed ❌")
```

### ✅ `or` (any one True)

```python
is_admin = False
is_manager = True

if is_admin or is_manager:
    print("Access granted ✅")
```

### ✅ `not` (reverse)

```python
is_banned = False

if not is_banned:
    print("Welcome ✅")
```

---

# ✅ 8) Nested if (Condition inside condition)

```python
age = 25
country = "Bangladesh"

if age >= 18:
    if country == "Bangladesh":
        print("You can vote in BD ✅")
    else:
        print("Voting rules depend on your country 🌍")
else:
    print("Too young to vote ❌")
```

---

# ✅ 9) One-line if (Short form)

✅ Only use when it is simple.

```python
age = 19
print("Adult") if age >= 18 else print("Not Adult")
```

---

# ✅ Practice Task ✅ (Must Do)

### ✅ Task 1:

Take a number input and check:

* If number is positive → print `"Positive"`
* If negative → print `"Negative"`
* Else → print `"Zero"`

✅ Example:

```
Enter number: -5
Negative
```

---

### ✅ Task 2:

Take marks input (0-100) and print grade:

* 80-100 → A+
* 70-79 → A
* 60-69 → A-
* 50-59 → B
* Below 50 → Fail

